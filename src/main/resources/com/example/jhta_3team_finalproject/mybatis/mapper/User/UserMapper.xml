<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.jhta_3team_finalproject.mybatis.mapper.User.UserMapper">

    <select id="getUserId" >
        SELECT * FROM users WHERE user_id = #{userId}
    </select>

    <insert id="join" >
        insert  into users
        (user_num,user_id, user_password, user_name, user_email,user_phone_number,user_department,user_gender)
        values ( #{userNum},#{userId},#{userPassword} ,#{userName},#{userEmail},#{userPhoneNumber}, #{userDepartment},#{userGender})
    </insert>


    <update id="update">
        UPDATE users
        SET
            user_name            = #{userName},
            user_email           = #{userEmail},
            user_phone_number    = #{userPhoneNumber},
            user_gender          = #{userGender},
            user_profile_picture = #{userProfilePicture}
        WHERE user_num = #{userNum}
    </update>

    <!-- 사용자 정보 추가 -->
    <insert id="insert">
        insert  into users
        (user_num,user_id, user_password, user_email, user_name, user_phone_number, user_department, user_position, user_gender, user_profile_picture, user_created_at, user_updated_at, user_auth)
        values (#{userNum},#{userId}, #{userPassword}, #{userEmail}, #{userName}, #{userPhoneNumber}, #{userDepartment}, #{userPosition}, #{userGender}, #{userProfilePicture},  NOW(), NOW(), #{userAuth})
    </insert>

    <select id="getListCount" resultType="int">  <!-- 페이징 -->
        select  count(*) from users
    </select>
</mapper>